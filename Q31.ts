const users: string[] = []; 

if (users.length === 0) {
    console.log("We need to find some users!");
} else {
    // Remove all usernames from the array if needed
    users.length = 0;
    console.log("All usernames have been removed.");
}
